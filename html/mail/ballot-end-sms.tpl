La consultation suivante organisée par {$ballot.asker_name} est terminée : {$ballot.ballot_title}
Pour voir les résultats, rendez-vous ici : {$smarty.env.APP_HOST}/{cp_mail_shortcode($to,"/r/{$ballot.ballot_shortcode}",$ballot.ballot_id)}